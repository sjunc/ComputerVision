setGeometry(시작x, 시작y, 크기, 크기)
self.label.setText
콤보박스 pickCombo
ds.mnist.load_data()
model.save('.h5')
.load_model()
model.predict()
LeNet-5
기본 stride = 값
model.compile()
model.fit() 

models  Sequential / Functional  더 자유로움. 
crossentropy 두 확류의 다른 정도 
Focal Loss
불균형 데이터
객체 탐지에서 객체는 일부고 배경이 대부분 
-> 가중치를 객체에 더 주고 

백본 모델
vgg16
googleNet - inception 모듈-네트워크안에 네트워크
resnet 마이크로소프트, 숏컷 연결

11주차

분류, 검출, 분할, 추적, 행동 분류


분류
범주분류 - 사례분류 (잘려있는 걸 보고 검출과 덧붙여서 사용)
분할
semantic segmentation (의미)
instance segmentation(사례)
추적
vot 하나 mot 다중
IoU
교집합 / 합집합
일반적 0.75

신뢰도 순으로 정렬
정답 박스와 비교 
임계값 iou
pr curve (가로 recall)
선 밑 영역이 ap
평균 map

이미지 분할 U-net  지름길 연결

동적 비전
영상 -> 3차원 시간축 4차원
흐름 optical flow

추적
tracking
고전 KLT tracker
특징점 기반, 밝기가 변하지 않음  

실시간 - online
과거 녹화 - batch 

single
vot, mot
multi 

box tracking 바운딩 박스
mask tracking 영역 정확히
처리 과정
검출 -> 특징 추출 -> 거리계산 -> 쌍 맺기 

나갔다가 들어오면 번호가 늘어남(SORT)

자세
인체 표현 skeleton , volume
사람 -> 관절 (Top-down)
관절 -> 사람 (Bottom-Up)

ViT
Attention is all you need
인코더  - 디코더 
전체 한반에 - 병렬처리, 속도 빠름

self-attention -> multihead-self-attention

vit
이미지 작은 단위 patch 로 나눔  
영상 분류 인코더만  사용

from_pretrained

DETR 트랜스포머 원래 유지, 박스 집합 예측

point cloud 3차원 
순서없는 점들이 많음 Lidar, streo camera(양안 시차)

pointNet
3d 객체 분류, 3d 객체 분할, Scene Segmetntation
T-Net 2번 나타나고 정규화 역할 
2번째 T-Net 특징 공간에서 정렬 mlp - 고수준 특징

생성모델 
GMM (gausian Mixture Model, 가우시안 혼합 모델): 여러개 가우시안 조합 
생성모델: 데이터의 분포를 따르는 새로운 샘플을 만듦

평균, 공분산, 만들 샘플값 

Autoencoder
원래 이미지 -> 인코더 -> latent vector -> 디코더 -> 복원
Conv2dTranspose
latent vector
두개 샘플 1,6 사이에서 특징을 뽑아서 넣기 

latent vector 확률적 분포따르게 
